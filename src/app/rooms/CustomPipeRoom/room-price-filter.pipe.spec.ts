import { RoomPriceFilterPipe } from './room-price-filter.pipe';

describe('RoomPriceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomPriceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
